import Logo from "./wreath.png";

export default function () {
  return `
            <header class="header">
                <figure>
                    <img src=${Logo} alt="Logo de ArtShop">
                </figure>
                <nav id="header-nav">
                </nav>
            </header>
        
        `;
}
